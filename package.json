{
  "name": "taglib2",
  "version": "2.9.0",
  "description": "taglib version2 bindings",
  "scripts": {
    "test": "tape ./tests",
    "install": "cmake-js rebuild -r $npm_config_runtime -v $npm_config_target --abi $npm_config_abi",
    "rebuild": "npm run install"
  },
  "dependencies": {
    "cmake-js": "2",
    "nan": "^2.4.0"
  },
  "devDependencies": {
    "mkdirp": "^0.5.1",
    "rimraf": "^2.5.2",
    "tape": "*"
  }
}
